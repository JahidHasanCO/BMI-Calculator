name: Test, Review and Generate APK
on:
  push:
    branches: [ "master" ]
  pull_request:
    # The branches below must be a subset of the branches above
    branches: [ "master" ]

jobs:

  send-apk-on-telegram:
    name: Send Apk on Telegram
    runs-on: ubuntu-latest
    steps:
      - name: send telegram message on push
        uses: appleboy/telegram-action@master
        with:
          to: "-677728639"
          token: "5778849475:AAEuVVY0-fA5361Zkv1tSBfnQYujCQCc1kA"
          message: Hi, This is test messege from github Action
          message_file: app/build.gradle.kts
        
           
        
           


